name: E2E Tests

on:
  workflow_dispatch:
  schedule:
    - cron: "15 * * * *"

jobs:
  check:
    # Determine if tests should run. For scheduled triggers, skip
    # if no commits in the last hour. Always run for workflow_dispatch.
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
    steps:
      - uses: actions/checkout@v6

      - name: Check for recent commits
        id: check
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "should_run=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          LAST_COMMIT=$(git log -1 --format=%ct)
          NOW=$(date +%s)
          DIFF=$((NOW - LAST_COMMIT))
          if [ $DIFF -le 3600 ]; then
            echo "Commits found in the last hour"
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "No commits in the last hour, skipping"
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

  test:
    needs: check
    if: needs.check.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      id-token: write
      contents: read
    env:
      OLLAMA_API_KEY: ${{ secrets.OLLAMA_API_KEY }}
      OLLAMA_BASE_URL: ${{ secrets.OLLAMA_BASE_URL }}
      OLLAMA_MODEL: ${{ secrets.OLLAMA_MODEL }}
      BARK_JWT_SECRET: e2e-test-secret
      BARK_DEFAULT_USER: admin
      BARK_DEFAULT_PASSWORD: admin
    steps:
      - uses: actions/checkout@v6

      - uses: DeterminateSystems/determinate-nix-action@main

      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Free disk space
        run: |
          set -x
          rm -rf $HOME/.cache
          nohup sudo ionice -c3 nice -n19 rm -rf \
            /usr/share/dotnet /usr/local/lib/android \
            /opt/hostedtoolcache /usr/lib/jvm \
            /usr/share/swift $HOME/.rustup \
            /usr/local/share/chromium \
            /usr/lib/google-cloud-sdk \
            /usr/local/powershell /usr/local/.ghcup \
            /usr/local/julia* &

      - name: Create .env
        run: |
          cat > .env << EOF
          OLLAMA_API_KEY=${{ secrets.OLLAMA_API_KEY }}
          OLLAMA_BASE_URL=${{ secrets.OLLAMA_BASE_URL }}
          OLLAMA_MODEL=${{ secrets.OLLAMA_MODEL }}
          BARK_JWT_SECRET=e2e-test-secret
          BARK_DEFAULT_USER=admin
          BARK_DEFAULT_PASSWORD=admin
          BARK_TEST_MODE=1
          EOF

      - name: Install devenv
        run: ./bootstrap --unattended

      - name: Start services and wait for server
        shell: bash
        run: |
          . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
          devenv up --no-tui > /tmp/devenv-up.log 2>&1 &
          DEVENV_PID=$!
          echo "devenv up started as PID $DEVENV_PID"
          for i in $(seq 1 300); do
            if ! kill -0 $DEVENV_PID 2>/dev/null; then
              echo "devenv up exited unexpectedly, logs:"
              cat /tmp/devenv-up.log
              exit 1
            fi
            code=$(curl -s -o /dev/null -w "%{http_code}" \
              http://localhost:8997/ 2>/dev/null || true)
            if [ "$code" = "200" ]; then
              echo "Server ready (attempt $i)"
              exit 0
            fi
            echo "Waiting for server... (attempt $i, status=$code)"
            echo -e "\033[32m$(tail -1 /tmp/devenv-up.log 2>/dev/null)\033[0m"
            sleep 2
          done
          echo "Server failed to start after 10 minutes, logs:"
          cat /tmp/devenv-up.log
          exit 1

      - name: Install Playwright browsers
        run: |
          . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
          devenv shell -- bash -c \
            "cd src/e2e_tests && npm run install-browsers"

      - name: Run E2E tests
        run: |
          . /nix/var/nix/profiles/default/etc/profile.d/nix-daemon.sh
          CHROME=$(which google-chrome-stable 2>/dev/null \
            || which chromium-browser 2>/dev/null \
            || echo /usr/bin/google-chrome-stable)
          devenv shell -- bash -c \
            "cd src/e2e_tests && \
             CHROME_PATH=$CHROME npx playwright test"

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-results
          path: src/e2e_tests/test-results/
